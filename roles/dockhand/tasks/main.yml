---
- name: Validate state parameter
  ansible.builtin.fail:
    msg: "dockhand_state must be one of: prepare, present, absent"
  when: dockhand_state not in ['prepare', 'present', 'absent']

- name: Prepare Dockhand
  when: dockhand_state == 'prepare'
  block:
    - ansible.builtin.include_tasks:
        file: "../_base/tasks/prepare.yml"

- name: Deploy Dockhand
  when: dockhand_state == 'present'
  block:
    - ansible.builtin.include_tasks:
        file: prepare.yml
    - ansible.builtin.include_tasks:
        file: "../_base/tasks/secrets.yml"
    - ansible.builtin.include_tasks:
        file: "../_base/tasks/compose_deploy.yml"

- name: Remove Dockhand
  when: dockhand_state == 'absent'
  block:
    - ansible.builtin.include_tasks:
        file: "../_base/tasks/cleanup.yml"
      vars:
        service_state: absent
