---
- name: Validate state parameter
  ansible.builtin.fail:
    msg: "gitea_state must be one of: prepare, present, absent"
  when: gitea_state not in ['prepare', 'present', 'absent']

- name: Prepare Gitea
  when: gitea_state == 'prepare'
  block:
    - ansible.builtin.include_tasks:
        file: "../_base/tasks/prepare.yml"

- name: Deploy Gitea
  when: gitea_state == 'present'
  block:
    - ansible.builtin.include_tasks:
        file: prepare.yml
    - ansible.builtin.include_tasks:
        file: "../_base/tasks/secrets.yml"
    - ansible.builtin.include_tasks:
        file: "../_base/tasks/compose_deploy.yml"

- name: Remove Gitea
  when: gitea_state == 'absent'
  block:
    - ansible.builtin.include_tasks:
        file: "../_base/tasks/cleanup.yml"
      vars:
        service_state: absent
