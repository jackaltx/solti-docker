---
- name: Validate state parameter
  ansible.builtin.fail:
    msg: "ladder_state must be one of: prepare, present, absent"
  when: ladder_state not in ['prepare', 'present', 'absent']

- name: Prepare Ladder
  when: ladder_state == 'prepare'
  block:
    - ansible.builtin.include_tasks:
        file: "../_base/tasks/prepare.yml"

- name: Deploy Ladder
  when: ladder_state == 'present'
  block:
    - ansible.builtin.include_tasks:
        file: prepare.yml
    - ansible.builtin.include_tasks:
        file: "../_base/tasks/secrets.yml"
    - ansible.builtin.include_tasks:
        file: "../_base/tasks/compose_deploy.yml"

- name: Remove Ladder
  when: ladder_state == 'absent'
  block:
    - ansible.builtin.include_tasks:
        file: "../_base/tasks/cleanup.yml"
      vars:
        service_state: absent
