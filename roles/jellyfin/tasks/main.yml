---
- name: Validate state parameter
  ansible.builtin.fail:
    msg: "jellyfin_state must be one of: prepare, present, absent"
  when: jellyfin_state not in ['prepare', 'present', 'absent']

- name: Prepare Jellyfin
  when: jellyfin_state == 'prepare'
  block:
    - ansible.builtin.include_tasks:
        file: "../_base/tasks/prepare.yml"

- name: Deploy Jellyfin
  when: jellyfin_state == 'present'
  block:
    - ansible.builtin.include_tasks:
        file: prepare.yml
    - ansible.builtin.include_tasks:
        file: "../_base/tasks/secrets.yml"
    - ansible.builtin.include_tasks:
        file: "../_base/tasks/compose_deploy.yml"

- name: Remove Jellyfin
  when: jellyfin_state == 'absent'
  block:
    - ansible.builtin.include_tasks:
        file: "../_base/tasks/cleanup.yml"
      vars:
        service_state: absent
