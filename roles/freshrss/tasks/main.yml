---
- name: Validate state parameter
  ansible.builtin.fail:
    msg: "freshrss_state must be one of: prepare, present, absent"
  when: freshrss_state not in ['prepare', 'present', 'absent']

- name: Prepare FreshRSS
  when: freshrss_state == 'prepare'
  block:
    - ansible.builtin.include_tasks:
        file: "../_base/tasks/prepare.yml"

- name: Deploy FreshRSS
  when: freshrss_state == 'present'
  block:
    - ansible.builtin.include_tasks:
        file: prepare.yml
    - ansible.builtin.include_tasks:
        file: "../_base/tasks/secrets.yml"
    - ansible.builtin.include_tasks:
        file: "../_base/tasks/compose_deploy.yml"

- name: Remove FreshRSS
  when: freshrss_state == 'absent'
  block:
    - ansible.builtin.include_tasks:
        file: "../_base/tasks/cleanup.yml"
      vars:
        service_state: absent
