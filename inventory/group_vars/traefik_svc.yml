---
traefik_acme_email: "jack@lavnet.net"
traefik_log_level: "WARN"
traefik_linode_token: "{{ lookup('env', 'LINODE_TOKEN') }}"
# Raw bcrypt hash (single $) — template escapes to $$ for compose label parsing
# Generate: echo $(htpasswd -nb user password) | sed -e s/\\$/\\$\\$/g  (then remove the extra $)
traefik_dashboard_auth: "{{ lookup('env', 'TRAEFIK_DASHBOARD_AUTH') }}"
