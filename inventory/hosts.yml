---
# solti-docker inventory
# Group membership only — all host vars live in host_vars/<hostname>.yml

all:
  children:
    docker_hosts:
      hosts:
        dockarr:
        truenas:

      children:
        # ====================================================================
        redis_svc:
          hosts:
            dockarr:

        # ====================================================================
        dozzle_svc:
          hosts:
            dockarr:

        # ====================================================================
        cyberchef_svc:
          hosts:
            dockarr:

        # ====================================================================
        it_tools_svc:
          hosts:
            dockarr:

        # ====================================================================
        ladder_svc:
          hosts:
            dockarr:

        # ====================================================================
        freshrss_svc:
          hosts:
            dockarr:

        # ====================================================================
        homarr_svc:
          hosts:
            dockarr:

        # ====================================================================
        gitea_svc:
          hosts:
            dockarr:

        # ====================================================================
        arcane_svc:
          hosts:
            dockarr:

        # ====================================================================
        minio_svc:
          hosts:
            dockarr:

        # ====================================================================
        conduit_svc:
          hosts:
            dockarr:

        # ====================================================================
        obsidian_svc:
          hosts:
            dockarr:

        # ====================================================================
        traefik_svc:
          hosts:
            dockarr:

        # ====================================================================
        jellyfin_svc:
          hosts:
            dockarr:

        # ====================================================================
        rustfs_svc:
          hosts:
            dockarr:

        # ====================================================================
        mongodb_svc:
          hosts:
            dockarr:

        # ====================================================================
        vpn_torrent_svc:
          hosts:
            truenas:

        # ====================================================================
        arr_stack_svc:
          hosts:
            truenas:
